# Runtime for the GPU-edition lecture notebooks — they are self-contained: they import
# torch + numpy + matplotlib + astropy and load the shipped reference/*.npz data files (the NumPy
# edition's references). No pykurucz, no production kgpu import.
torch          # the GPU array library: Apple MPS / CUDA, with a CPU fp64 fallback
numpy          # the reference array library + the validation comparisons
matplotlib
astropy        # physical constants and unit conversion

# Building the book (assembling notebooks with nbformat, executing with nbconvert):
nbformat
nbconvert
jupyter

# Rendering the executed notebooks to HTML fragments uses Node, not Python:
#   npm install        # installs marked / katex / highlight.js (see package.json)
#   python _pipeline/build.py 1 2 3 4 5 6 7 8 9
