# Core Payne Zero implementation taught by the book.
#
# These lower bounds match the pinned Payne Zero package.  Atmosphere physics
# uses NumPy + Numba; synthesis and the learned initializers use PyTorch.
numba>=0.59
numpy>=1.26
torch>=2.2

# Executable textbook and publication-quality quantitative figures.
jupyterlab>=4.2
nbconvert>=7.16
matplotlib>=3.8

# Repository verification.
pytest>=8.2
