# Chapter 4 Acceptance Record

Acceptance date: 2026-07-29

Scope: chapter-level acceptance. Chapter 4 remains subject to later
whole-book notation, redundancy, coverage, and capstone-parity audits.

## Outcome

Chapter 4 is accepted as the molecular thermochemistry and atmosphere-to-
synthesis handoff slice. A reader now builds the result in one causal sequence:

1. a two-body association reaction couples two elemental ledgers;
2. particle, elemental, and charge conservation generalize that toy gas to the
   exact 23-density network;
3. analytic and automatically differentiated Jacobians turn the coupled
   residual into backend-specific Newton steps;
4. exact damping, positivity, cold starts, and pressure-scaled continuation
   keep both depth chains explicit;
5. atmosphere and synthesis routes retain distinct electron, molecule, mass,
   precision, and failure contracts;
6. the 170- and 190-record catalogs align by code and component semantics;
7. 54 normalized molecular line populations enter the synthetic stage-5 lane
   while the actual ion-stage cube remains atomic; and
8. the solved atmosphere handoff supplies the electron column for the
   demonstrated schema-v4 release state.

There is no detached exercise section. Limiting cases, controlled failures,
branch boundaries, temperature/pressure predictions, dtype differences, and
parity checks are resolved where they become physically necessary.

## Gates passed

- 141 Chapter 4, source, oracle, parity, manifest, schematic-provenance, and
  book-governance tests pass together.
- The canonical notebook executes from a clean kernel with no error or stderr
  output.
- Exactly 17 visible scientific code cells remain; every one is at most 35
  lines, has no line longer than 92 characters, and is bridged by interpreted
  prose.
- The five comparison-only CPU-`float64` golden archives are identity-bound,
  independently captured, byte-stable, and opened only after local results
  exist.
- Atmosphere and synthesis 23-by-23 Jacobians pass independent finite-
  difference checks; the direct and rank-deficient atmosphere linear-solver
  branches, both positivity policies, and the synthesis nonfinite-residual
  guard execute visibly.
- Both backends retain ordered pressure-scaled depth continuation; Numba,
  LAPACK, `jacrev`, and post-loop `vmap` ownership is stated without inventing
  a batched molecular Newton solve.
- Full, fixed-electron, disabled, structured-handoff, live diagnostic,
  fallback, and release routes have distinct public/internal ownership claims.
- Formation branches, frozen/recomputed constant lifecycle, cold starts,
  abundance floors, tolerances, last-iterate exhaustion, and route-specific
  device/dtype behavior are explicit.
- All 170 atmosphere records align semantically with synthesis; the synthesis
  catalog adds exactly 20 records and 64 shared records occupy different rows.
- All 54 public molecular mappings pass with the exact 51/3 column split.
  CO independently reconstructs `276 → 608 → [depth, 5, 45]`; grounded and
  no-ground partition policies differ; the actual cube remains unchanged.
- The 27-field schema-v4 inventory, shapes, version, and solved-atmosphere
  electron ownership close the Chapter 5 handoff.
- Four original conceptual schematics have owned prompts, hashes, alt text,
  captions, and scientific review. Both quantitative figures are professional,
  one-panel, and one-claim.
- The local reader renders the accepted chapter cleanly. A direct link to the
  planned Chapter 5 now produces an honest in-construction page rather than a
  missing-file error.

## Independent-audit closure

The final independent science and pedagogy audits both returned **ACCEPT** with
no remaining P0/P1 finding. Their prior blockers were closed by:

- deriving the general 23-variable residual and exposing the stable
  association calculation;
- rebuilding and moving the Newton schematic so damping precedes candidate
  formation and every label is earned before use;
- feeding the solved atmosphere electron column into the release checkpoint;
- distinguishing diagnostic bridge, fallback, and release catalog paths;
- exposing nonfinite, tolerance, exhaustion, seed, and dtype policies;
- separating abundance notation from atomic/molecular mass notation; and
- synchronizing the canonical source, executed notebook, rendered HTML, and
  planned-chapter reader behavior.

Chapter 5 may now consume the closed population state without reopening
molecular equilibrium.
