.venv/
node_modules/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
# scratch / logs only — the reference/*.npz data files ARE committed (the book is self-contained)
reference/*.log
/tmp/
# internal LLM-critic QA output, not book content
_pipeline/critic_reports/
_pipeline/_proof_mol_numba.py
# port-worker scratch: candidate torch modules + per-try run logs (regenerable; the accepted
# kernel is transcribed into build_lectureN_gpu.py, the source of truth)
_pipeline/_ports/
# internal dev planning / scratch docs (kept in git history, not in the published tree)
_pipeline/PLAN_*.md
# regenerable dev QA check-plots written by verify_*.py — the book figures live in resources/figures/
_pipeline/*.png
